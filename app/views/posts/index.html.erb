<h1>Mis posts</h1>
	<table>
		<tr> <th>Titulo</th> <th>Texto</th>
		<% @posts.each do |post| %>
		<th></th>
			<th> </th>
		<th></th>
	<tr>
		<td><%= post.title %></td>
		<td><%= post.text %></td>
		<td><%= link_to 'Show', post_path(post) %></td>
		<td><%= link_to 'Edit', edit_post_path(post) %></td>
		<td><%= link_to 'Destroy', post_path(post), method: :delete, data: {confirm: '¿Seguro?'} %></td>
	</tr>
	<% end %>
</table>
<%= link_to 'Back', posts_path %>
<%= link_to 'New', new_post_path %>