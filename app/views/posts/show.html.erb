<h1>Este post tiene estas cosas:</h1>
<p>
	<strong>Title:</strong>
	<%= @post.title %>
	</p>
<p>
	<strong>Text:</strong>
	<%= @post.text %>
</p>

<h2>Comentarios</h2>
<%= render @post.comments %>

<h3>Agrega un comentario:</h3>
<%= render "comments/form" %>

<%= link_to 'Back', posts_path %>
<%= link_to 'Edit', edit_post_path(@post) %>